import {populateData} from '../src/client/js/app'

describe("Testing functionality of populateData which updates UI", () => {
    test("Testing the populateData() function", () => {
        expect(populateData).toBeDefined();
    })
  })
